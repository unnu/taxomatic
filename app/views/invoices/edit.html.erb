<h1>Bearbeite Rechnung</h1>

<%= form_for(@invoice, :html => {data: {:'with-tax' => true}}) do |form| %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Speichern' %>
<% end %>

<%= link_to 'zurück zur Liste', invoices_path %>
